<p id="notice"><%= notice %></p>
<br>
<%= render "/shared/navbar" %>
<br>
<br>
<div class="entry-card" style="width: 30rem;">
  <div class="entry-card-body">
    <% if @user_profile.user_id == current_user.id %>
    <h1>Your Profile</h1>
    <br>
        <h2>Main Details</h2>
        <p>
            <strong>First Name:</strong>
            <%= @user_profile.first_name %>
        </p>
        <p>
            <strong>First Name:</strong>
            <%= @user_profile.last_name %>
        </p>
        <p>
            <strong>Age</strong>
            <%= @user_profile.age %>
        </p>
        <p>
            <strong>Contact Number</strong>
            <%= @user_profile.contact_number %>
        </p>
        <br>
        <h2>Address Info</h2>
        <p>
            <strong>Street Number</strong>
            <%= @user_profile.address.street_number %>
        </p>
        <p>
            <strong>Street Name</strong>
            <%= @user_profile.address.street_name %>
        </p>
        <p>
            <strong>Suburb</strong>
            <%= @user_profile.address.suburb %>
        </p>
        <p>
            <strong>State</strong>
            <%= @user_profile.address.state %>
        </p>
        <p>
            <strong>Postcode</strong>
            <%= @user_profile.address.postcode %>
        </p>
            <div class="functions">
        <% if @user_profile.user_id == current_user.id %>
            <%= link_to 'Edit', edit_user_profile_path(@user_profile), class:"btn btn-warning" %></td>
        <% end %>
        <% if @user_profile.user_id == current_user.id || current_user.has_role?(:admin) %>
            <%= link_to 'Destroy', @user_profile, class:"btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      <%= link_to 'Back', root_path, class:"btn btn-primary" %>
    </div>
<% end %>

